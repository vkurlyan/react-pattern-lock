// import { fromJS } from 'immutable';
// import { makeSelectChangePatternDomain } from '../selectors';

// const selector = makeSelectChangePatternDomain();

describe('makeSelectChangePatternDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
